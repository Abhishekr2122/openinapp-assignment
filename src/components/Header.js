export default function Header() {
  return (
    <div
      style={{
        position: "relative",
        left: "-195px",
        border: "1px solid blue",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "32px",
      }}
    >
      This is the header
    </div>
  );
}
